# GEARHEAD dependencies
anthropic>=0.40        # Claude API (the brain)
python-dotenv>=1.0     # loads the secret key from .env
pymupdf>=1.24          # reads manual PDFs (text + images)
chromadb>=0.5          # local search database (the Library)
fastapi>=0.115         # the web app body
uvicorn>=0.30          # runs the local web server
